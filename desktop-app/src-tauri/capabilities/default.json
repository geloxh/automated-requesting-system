{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline capabilities for the main window. No filesystem, shell, or other privileged APIs are granted — this app is a plain webview pointed at a remote URL with no native Rust<->JS bridge in use.",
  "windows": ["main"],
  "permissions": ["core:default"]
}

<VirtualHost *:80>
    ServerName localhost
    DocumentRoot /var/www/automated-requesting-system/public

    # Redirect all HTTP > HTTPS for local testing
    RewriteEngine On
    RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]